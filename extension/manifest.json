{
  "manifest_version": 3,
  "name": "TrustLens",
  "version": "0.3.0",
  "description": "Trust & AI-visibility audit for service businesses. Detects the niche, boxes every element on the page, then scores SEO, AI search visibility, trust, authority and discoverability — judged by TypeSafe's Jev.",
  "permissions": ["activeTab", "tabs", "sidePanel", "scripting"],
  "host_permissions": ["http://*/*", "https://*/*"],
  "action": { "default_title": "TrustLens — scan this site" },
  "side_panel": { "default_path": "sidepanel.html" },
  "background": { "service_worker": "background.js" },
  "icons": { "128": "icon128.png" }
}
